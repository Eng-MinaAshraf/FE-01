# FE-01: Environment and AI Toolchain

This is the project repository for the FE-01 assignment.

## Setup
1. Clone the repository
2. Run `npm install` (if package.json is added in the future)
3. Start coding!
